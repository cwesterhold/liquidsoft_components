name: example
description: An example project using LiquidSoft Components

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  liquidsoft_components:
    git:
      url: https://github.com/cwesterhold/liquidsoft_components
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

